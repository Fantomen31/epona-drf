import React from 'react'
import styles from '../../styles/ClubProfilePage.module.css'

const ClubSectionTitle = ({ children }) => (
  <h2 className={styles.sectionTitle}>{children}</h2>
)

export default ClubSectionTitle